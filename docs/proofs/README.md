# Connection proof reports

Generated by `python tools/mp_prove.py --suite all --out docs/proofs/latest.json`.

- `latest.json` â€” last PASS/FAIL suite (connect + map-mismatch)

- `avatar-20260921/report.json` — independent movement/outfit acceptance, reconnect evidence and interpolation counters. Visual smoothing acceptance is explicitly separate.
